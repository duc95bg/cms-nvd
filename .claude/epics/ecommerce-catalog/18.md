---
name: "Data layer: HasLocalizedContent trait, migrations, models, formatPrice helper"
status: closed
created: 2026-04-16T06:23:24Z
updated: 2026-04-16T08:17:19Z
github: https://github.com/duc95bg/cms-nvd/issues/18
depends_on: []
parallel: true
conflicts_with: []
---

## Size: S (2h)

## Description

Foundation for the entire epic.

## Files to create

- `app/Traits/HasLocalizedContent.php` — trait with `t(string $column, ?string $locale = null, string $default = ''): string` that does `data_get($this->{$column}, $locale) ?? data_get($this->{$column}, $fallback) ?? $default`
- 7 migrations in order: categories, attributes, attribute_values, products, product_images, product_attributes (pivot), product_variants, variant_attribute_values (pivot)
- 6 models: `Category`, `Attribute`, `AttributeValue`, `Product`, `ProductImage`, `ProductVariant` — all with fillable, casts (json columns as array), relationships as specified in epic, and `use HasLocalizedContent` where applicable
- `app/Support/PriceFormatter.php` — `formatPrice(float $price, ?string $locale = null): string` returning `250.000₫` for vi, `$250,000.00` for en

## Acceptance criteria

- [ ] `HasLocalizedContent` trait with `t()` method
- [ ] 7+1 migrations (categories, attributes, attribute_values, products, product_images, product_attributes pivot, product_variants, variant_attribute_values pivot)
- [ ] 6 models with relationships: Category (self-ref parent/children, hasMany Product), Attribute (hasMany AttributeValue, belongsToMany Product), AttributeValue (belongsTo Attribute), Product (belongsTo Category, belongsToMany Attribute, hasMany ProductVariant, hasMany ProductImage, `getEffectivePrice()`, `getPriceRange()`), ProductVariant (belongsTo Product, belongsToMany AttributeValue), ProductImage (belongsTo Product)
- [ ] `PriceFormatter::format()` with locale-aware formatting
- [ ] `php artisan migrate` runs cleanly

## Definition of Done

Code implemented, migrate clean, trait tested in tinker
