---
name: Middleware SetLocale
status: open
created: 2026-04-15T00:00:00Z
updated: 2026-04-15T02:48:42Z
github: https://github.com/duc95bg/cms-nvd/issues/3
depends_on: [2]
parallel: true
conflicts_with: []
---

# Task 3 — Middleware SetLocale

## Mục tiêu
Tạo middleware đọc locale từ URL segment hoặc session và áp dụng vào request.

## Việc cần làm
1. Tạo `app/Http/Middleware/SetLocale.php`:
   - Constant `SUPPORTED = ['en', 'vi']`, `DEFAULT = 'vi'`.
   - Lấy `$request->segment(1)`.
   - Nếu thuộc SUPPORTED → set locale + `session()->put('locale', ...)`.
   - Ngược lại → đọc `session('locale', DEFAULT)`.
   - Gọi `app()->setLocale($locale)`.

## Acceptance criteria
- File tồn tại, namespace đúng `App\Http\Middleware`.
- Unit test (có thể viết cùng task 6): middleware set locale đúng với 4 case.
- Không query DB, không inject service ngoài `Request`.

## Files đụng tới
- `app/Http/Middleware/SetLocale.php` (new)
